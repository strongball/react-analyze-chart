import React from 'react';
import SimpleChart from '../components/SimpleChart';

interface Props {}
const JSOnlyPage: React.FC<Props> = (props) => {
  return <SimpleChart />;
};
export default JSOnlyPage;
